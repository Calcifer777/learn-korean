[project]
name = "learn-korean"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "elevenlabs>=2.36.1",
    "genanki>=0.13.1",
    "numpy>=2.4.2",
    "pandas>=3.0.1",
    "playsound3>=3.3.1",
    "python-dotenv>=1.2.1",
    "stable-ts[fw]>=2.19.1",
]

[project.scripts]
hello = "learn_korean:hello"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
